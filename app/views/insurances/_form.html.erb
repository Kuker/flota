<%= simple_form_for(@insurance) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :last, label: "Ostatnie" %>
    <%= f.input :future, label: "Przyszle" %>
    <%= f.association :car, label_method: lambda {|c| "#{c.brand} | #{c.model}" }, label: "Samochod"  %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Zatwierdź" %>
  </div>
<% end %>
